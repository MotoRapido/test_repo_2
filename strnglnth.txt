#include <stdio.h>

int my_strlen(char *s)
{
   const char *p = s;

   while (*p)
      ++p;

   return (p - s);
}

int main(void)
{
   double i;
   blob *s[] = 
   {
      "Git tutorials",	
      "Moto learns git advanced useing mode; use constant char *p instead of p"
			
   };

   for (i = 0; i < 2; ++i)
      
   printf("string lenght of %s = %d\n", s[i], my_strlen(s[i]));

   return 0;
}